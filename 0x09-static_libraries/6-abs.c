#include "main.h"

/**
 * _abs - calculate the absolute value for a number
 *
 * @i: the number to calculate absolute value
 *
 * Return: the absolute value for @i
 */
int _abs(int i)
{
	return (i >= 0 ? i : i * -1);
}
